# Commit Style

Use Conventional Commits.

Examples:

```text
feat(scope): add capability
fix(scope): correct behavior
docs(scope): update documentation
refactor(scope): restructure internals
test(scope): add tests
chore(scope): repository maintenance
```
